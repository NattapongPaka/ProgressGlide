<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:yourapp="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_refresh"
        yourapp:showAsAction="always"
        android:icon="@drawable/ic_more_vert_white_24dp"
        android:title="edit">

        <menu>
            <!--item
                android:id="@+id/action_take_a_photo"
                android:orderInCategory="100"
                android:title="Take a Picture"
                android:onClick="take_a_photo" /-->
            <item
                android:id="@+id/action_select_gallery"
                android:orderInCategory="200"
                android:title="Add Photos"
                android:onClick="select_gallery" />
            <item
                android:id="@+id/action_delete_photos"
                android:orderInCategory="200"
                android:title="Delete Photos"
                android:onClick="delete_photos" />
            <item
                android:id="@+id/action_rename_album"
                android:orderInCategory="200"
                android:title="Rename Album"
                android:onClick="rename_gallery" />

            <item
                android:id="@+id/action_delete_album"
                android:orderInCategory="200"
                android:title="Delete Album"
                android:onClick="delete_album" />
        </menu>

    </item>

</menu>
